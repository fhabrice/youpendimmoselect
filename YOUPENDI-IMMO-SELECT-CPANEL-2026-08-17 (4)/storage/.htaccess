# YOUPENDI IMMO SELECT — données locales (configuration, journaux, base SQLite)
# Rien de ce dossier ne doit être accessible depuis le web.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
